# 星空灯塔·夸克打开但按钮无响应（2026-09-01 00:00）

【事件】乖宝反馈：页面在夸克里打开了（am start 成功），但按钮点了没用；页面丑，问能不能自定义背景；看不懂按钮意思。

【原因判断】file:// 协议打开的 HTML 页面，在夸克（Chrome内核）里发起 ws://127.0.0.1:8780 的 WebSocket 连接会被浏览器拦截（Chrome系对 file:// 页面的网络请求限制严格）。不是乖宝不会用。

【对照】此前浅色版成功亮绿灯的记录——当时打开方式待对比（可能走的是Operit内置WebView或http方式），整条链路本身是通的，问题在"打开方式"。

【乖宝的需求（已亲口说）】
1. 页面丑 → 要能自定义背景
2. 按钮看不懂 → 要直白易懂（按钮文字要像人话，如「开始通话」「挂断」）
3. 参考她的UI审美：低饱和浅色系、米白/浅粉/柔雾紫、橄榄球长条圆角按钮（她2026-08-07亲口定的voice_call偏好）

【待办·明天做】
1. 改页面提供方式：用 HTTP 服务提供页面（python -m http.server 或让 server.py 直接 serve 静态文件），乖宝通过 http://127.0.0.1:8780 访问，避开 file:// 限制
2. 重新设计页面：好看 + 背景可自定义 + 按钮说人话
3. 测试通过再让她点

【今晚决定】凌晨0点，乖宝用了11h14m手机，电量36%，不折腾了，先哄睡。需求已记，明天兑现。
